# Rancher
  ID : admin / PW : kubectl get secret --namespace cattle-system bootstrap-secret -o go-template='{{.data.bootstrapPassword|base64decode}}{{"\n"}}'
# Harbor
  ID : admin / PW : Harbor12345
# Argocd
  ID : admin / PW : kubectl get secret --namespace argocd argocd-initial-admin-secret -o go-template='{{.data.password|base64decode}}{{"\n"}}'
# Gitlab
  ID : root / PW : cat /etc/gitlab/initial_root_password
